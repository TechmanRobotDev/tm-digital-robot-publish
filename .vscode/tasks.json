{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ubuntu - git push",
            "type": "shell",
            "command": "git add --all && git commit -m 'build for ubuntu' && git pull && git push",
            "problemMatcher": []
        },
        {
            "label": "windows - git push",
            "type": "shell",
            "command": "git add --all & git commit -m \"build for windows\" & git pull & git push",
            "problemMatcher": []
        }
    ]
}
